# AUTH_SECRET is required in production (and for `astro preview` / `npm start`).
# Random string, at least 16 characters (32+ recommended).
# Example: openssl rand -base64 32
AUTH_SECRET=

# Session cookies are Secure in production builds (default true when unset).
# Local `astro preview` / `npm start` over HTTP: false
# Railway HTTPS: true (or leave empty)
AUTH_COOKIE_SECURE=

# Directory for the first-party SQLite database (`app.db`: users, onboarding,
# plans, sessions, feedback, RunLogs, AdaptationEvents, Intervals connection
# status, magic link tokens). Used by the web app and `npm run adapt`. Railway: mount the volume
# at `.data` (see DEPLOY.md). Legacy `users.json` / `training.json` in this
# directory are imported once if the database is empty, then SQLite is the
# source of truth. Each user's Intervals access token or API key is stored
# only as AES-256-GCM ciphertext. It is never written in plaintext.
AUTH_DATA_DIR=.data

# Google OAuth (optional). OAuth 2.0 Web client in Google Cloud Console.
# When unset, "Continue with Google" shows: Couldn’t connect to Google. Try email or try again.
# Email + password still works.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Authorized redirect URI must match Google Cloud Console exactly.
# Local: http://localhost:4321/auth/google/callback
# Production: https://<your-public-host>/auth/google/callback
GOOGLE_CALLBACK_URL=http://localhost:4321/auth/google/callback

# Magic link email (optional). Resend HTTP API.
# When RESEND_API_KEY and MAIL_FROM are set, /login and /signup Email link sends
# "Your Stride Lab sign-in link". The Sign in button goes to /auth/magic?token=…
# (15 min, one-time). In local/dev (NODE_ENV !== production), if mail is unset the
# full sign-in URL is logged server-side and the UI still shows “Check your email”.
# In production, unset mail fails with “Couldn’t send the link. Try again.” and
# never logs the token or link.
RESEND_API_KEY=
MAIL_FROM=
# One-release fallback if MAIL_FROM is unset (prefer MAIL_FROM).
MAGIC_LINK_FROM=

# Required for HTTP POST/GET /api/adapt (Railway cron). The CLI `npm run adapt` does not need this.
# Random string, at least 16 characters recommended.
ADAPT_CRON_SECRET=

# LLM for the nightly adapt job and Today → Generate feedback.
# Recommended: OLLAMA_API_KEY and OLLAMA_MODEL. Host is https://ollama.com/v1.
# Model defaults to gemma4:31b when OLLAMA_MODEL is unset.
# Unset key: adapt stays on the heuristic, and Generate feedback tells the
# athlete to try again later (the UI never names these variables).
# If the key is set and the adapt call fails: log, do not mutate the plan, retry next run.
OLLAMA_API_KEY=
OLLAMA_MODEL=gemma4:31b
# Optional OpenAI-compatible override. When ADAPT_LLM_API_KEY is set it takes
# precedence over OLLAMA_API_KEY. Leave it unset or empty to use the Ollama path above.
ADAPT_LLM_API_KEY=
# ADAPT_LLM_BASE_URL and ADAPT_LLM_MODEL only apply when ADAPT_LLM_API_KEY is set.
# Defaults are https://api.openai.com/v1 and gpt-4o-mini.
# ADAPT_LLM_BASE_URL=https://api.openai.com/v1
# ADAPT_LLM_MODEL=gpt-4o-mini

# Encrypts each user's Intervals access token or API key at rest (AES-256-GCM,
# 32 bytes, unique IV per record, auth tag verified on read).
# Generate: openssl rand -base64 32
# Must be that standard base64 (44 characters, decodes to exactly 32 bytes).
# Hex and other formats are treated as missing.
# Required to connect. Missing or not 32 bytes: the Connect button stays visible
# but disabled ("Connecting Intervals.icu isn’t available right now. Try again later.")
# and nothing is stored in plaintext. Existing connections are left in place.
INTERVALS_KEY_ENC_SECRET=

# Intervals.icu OAuth (primary connect path). When both are set, Settings shows
# "Connect Intervals.icu". When either is missing, Settings shows the API key
# and athlete ID form instead.
# Create the app at https://intervals.icu/oauth/apply (must be approved before
# the flow works). Scopes, one per area (READ+WRITE on the same area fails
# with "Duplicate scope"; WRITE implies READ):
#   ACTIVITY:READ,CALENDAR:WRITE
# Redirect URI must match exactly. It is built from PUBLIC_ORIGIN (not Host or
# X-Forwarded-Host). Magic-link emails use the same origin. In production,
# OAuth is off and magic link send fails until PUBLIC_ORIGIN is set.
#   Local: http://localhost:4321/auth/intervals/callback
#   Production: https://running-stats-production.up.railway.app/auth/intervals/callback
PUBLIC_ORIGIN=https://running-stats-production.up.railway.app
INTERVALS_CLIENT_ID=
INTERVALS_CLIENT_SECRET=

# Optional owner fallback. Ignored unless INTERVALS_OWNER_ENV_FALLBACK=true
# and the signed-in email is in INTERVALS_OWNER_EMAILS (and verified, if that
# column exists). Everyone else connects their own key from Settings.
# Basic auth: username `API_KEY`, password = this key.
# HTTP calls send User-Agent `RunningStatsMVP/0.1`.
INTERVALS_ICU_API_KEY=
INTERVALS_ICU_ATHLETE_ID=i123456
INTERVALS_OWNER_ENV_FALLBACK=

# Emails allowed to use the shared env key above. Comma-separated.
# Case-insensitive; whitespace around each address is ignored.
# Unset or empty: the env fallback matches nobody.
# The account must also have a verified email (Google with email_verified, or a
# consumed magic link). Password signup does not verify. Existing rows are not
# backfilled. This allowlist does not gate a user's own OAuth token or pasted key.
# INTERVALS_OWNER_EMAILS=crispal94@gmail.com
INTERVALS_OWNER_EMAILS=
